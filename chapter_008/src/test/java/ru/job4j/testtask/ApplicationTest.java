package ru.job4j.testtask;

/*import org.junit.Test;

import java.sql.Timestamp;

import java.text.ParseException;
import java.text.SimpleDateFormat;
import java.util.Calendar;
import java.util.Locale;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.assertThat;

public class ApplicationTest {

    private final SimpleDateFormat format = new SimpleDateFormat("d MMM yy, HH:mm", new Locale("ru", "RU"));
    @Test
    public void whenParseVacancyThenParsingOnlyJava() {
        Calendar calendar = Calendar.getInstance();

        //System.out.println(System.currentTimeMillis());
        System.out.println(calendar.get(1));
        System.out.println(calendar.get(2));
        System.out.println(calendar.get(5));
        try {
            calendar.setTime(format.parse("31 янв 18, 22:08"));
        } catch (ParseException e) {
            e.printStackTrace();
        }
        System.out.println(new Timestamp(calendar.getTimeInMillis()));
    }
}*/