<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
             http://www.springframework.org/schema/beans/spring-beans.xsd
             http://www.springframework.org/schema/security
             http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="true">
        <intercept-url pattern="/add" access="hasRole('ROLE_ADMIN')"/>
        <intercept-url pattern="/" access="permitAll()"/>
        <form-login login-page="/login"
                    default-target-url="/add"
                    authentication-failure-url="/fvsdfsdfsd"
                    username-parameter="login"
                    password-parameter="password"
                    login-processing-url="/xvrgbgxrx"/>

        <csrf disabled="true"/>
    </http>

    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query="SELECT login, password FROM users
                               WHERE login=?"
                               authorities-by-username-query="SELECT u.login, r.name FROM roles AS r,
                               users as u WHERE r.id=u.role_id AND u.login=?"/>
        </authentication-provider>
    </authentication-manager>
</beans:beans>